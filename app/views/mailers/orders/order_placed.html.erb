<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <title>RailsCasts Weekly</title>
  <style type="text/css">
  body, td { color:#000000; font-size:14px; font-family:Verdana,Helvetica,Arial; }
  .divider { background-color:#555555; height:2px; }
  .episode h2 { margin-top:0; margin-bottom:8px; font-size:18px; }
  .episode p { margin:8px 0; }
  .episode img { border:solid 1px #777777; }
  .episode .duration { font-size:11px;color:#555555; }
  #footer { font-size:11px; color:#555555; }
  #footer a { color:#555555;}
  </style>
</head>
<body>
  <p>
    <b>Имя: <%= @order.fio %></b> <br>
    <b>Email: <%= @order.email %></b>  <br>
    <b>Телефон: <%= @order.phone %></b>  <br>
    <b>Комментарий: <%= @order.comment %></b>  <br>
  </p>
  <table width="100%" id="background" border="1" cellpadding="0" cellspacing="0">
    <tr>
      <th colspan="7">ORDER NUMBER - <%= @order.id %> </th>
    </tr>
    <tr>
      <td>Название</td>
      <td>Крепкость</td>
      <td>Кол-во</td>
    </tr>
    <% @order.details.each do |detail|  %>
      <tr>
        <td><%= detail.product.title %></td>
        <td><%= detail.nicotine.count %></td>
        <td><%= detail.product_count %></td>
      </tr>
    <%end%>
  </table>
  <p>
    Итого: <%= @order.totalPrice %> <br>
  </p>
</body>
</html>
